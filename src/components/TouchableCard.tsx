import React from 'react';
import {Text, View} from 'react-native/types';

const TouchableCard = () => {
  return (
    <View>
      <Text>Card Info</Text>
    </View>
  );
};

export default TouchableCard;
